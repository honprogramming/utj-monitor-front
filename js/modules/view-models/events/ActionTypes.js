define(
        function () {
            var actionTypes = {
                ADD: "add_action",
                REMOVE: "remove_action",
                EDIT: "edit_action"
            };
            
            return actionTypes;
        }
);