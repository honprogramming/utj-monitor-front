define([],
        function () {
            var EventTypes = {
                    CLICK_EVENT: "click_event",
                    SELECTION_EVENT: "selection_event"
            };
            
            return EventTypes;
        }
);