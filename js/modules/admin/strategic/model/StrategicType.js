define(
        function() {
            function StrategicType(id, name) {
                this.id = id;
                this.name = name;
            }
            
            return StrategicType;
        }
);